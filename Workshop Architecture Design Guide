📚 Library Management System – Architecture Documentation
📋 Project Information
Student Name: นางสาว กันติชา เกิดสี
Student ID: 675432100526
Course: ENGSE207 Software Architecture
System: Library Management System
Architecture Style: Layered Architecture (3-Tier)

🎯 System Overview
ระบบ Library Management System เป็นระบบสำหรับจัดการหนังสือภายในห้องสมุด
รองรับการเพิ่ม แก้ไข ลบ ยืม และคืนหนังสือ พร้อมแสดงสถิติการใช้งาน

🧩 C1: System Context Diagram
┌─────────────────────────────────────────────┐
│                 Library User                │
│        (Student / Librarian / Admin)        │
└───────────────┬─────────────────────────────┘
                │
                │ HTTP / JSON
                │ (CRUD, Borrow, Return)
                ▼
┌─────────────────────────────────────────────┐
│          Library Management System           │
│                                             │
│ • Manage books (CRUD)                        │
│ • Borrow / Return books                     │
│ • Filter by status                           │
│ • Display statistics                        │
│                                             │
└───────────────┬─────────────────────────────┘
                │
                │ SQL Queries
                ▼
┌─────────────────────────────────────────────┐
│              SQLite Database                │
│               (library.db)                  │
└─────────────────────────────────────────────┘

Actors
Library User: ผู้ใช้งานระบบ เช่น นักศึกษา บรรณารักษ์ และผู้ดูแลระบบ
External System
SQLite Database: ใช้เก็บข้อมูลหนังสือและสถานะการยืม

🏗️ C2: Container Diagram – Layered Architecture
┌───────────────────────────────────────────────┐
│              CLIENT (Browser)                 │
│           HTML / CSS / JavaScript             │
└───────────────┬───────────────────────────────┘
                │ HTTP / JSON
                ▼
╔═══════════════════════════════════════════════╗
║        LIBRARY MANAGEMENT SYSTEM (Node.js)    ║
╠═══════════════════════════════════════════════╣
║                                               ║
║ ┌───────────────────────────────────────────┐ ║
║ │        📋 PRESENTATION LAYER              │ ║
║ │                                           │ ║
║ │ • Routes (bookRoutes.js)                  │ ║
║ │ • Controllers (bookController.js)         │ ║
║ │ • Middlewares (errorHandler.js)            │ ║
║ │                                           │ ║
║ └──────────────────┬────────────────────────┘ ║
║                    │                          ║
║                    ▼                          ║
║ ┌───────────────────────────────────────────┐ ║
║ │        🧠 BUSINESS LOGIC LAYER             │ ║
║ │                                           │ ║
║ │ • Services (bookService.js)               │ ║
║ │ • Validators (bookValidator.js)           │ ║
║ │                                           │ ║
║ │ Business Rules:                           │ ║
║ │ ✓ title, author, isbn required            │ ║
║ │ ✓ book status: available / borrowed       │ ║
║ │ ✓ cannot borrow borrowed book             │ ║
║ │                                           │ ║
║ └──────────────────┬────────────────────────┘ ║
║                    │                          ║
║                    ▼                          ║
║ ┌───────────────────────────────────────────┐ ║
║ │        💾 DATA ACCESS LAYER                │ ║
║ │                                           │ ║
║ │ • Repository (bookRepository.js)          │ ║
║ │ • DB Connection (connection.js)           │ ║
║ │                                           │ ║
║ │ CRUD Operations                           │ ║
║ │ • findAll(status)                         │ ║
║ │ • findById(id)                            │ ║
║ │ • create(book)                            │ ║
║ │ • update(id, book)                        │ ║
║ │ • delete(id)                              │ ║
║ └──────────────────┬────────────────────────┘ ║
╚════════════════════╪═══════════════════════════╝
                     │
                     ▼
           ┌───────────────────────────┐
           │     SQLite Database        │
           │       (library.db)         │
           │                           │
           │ Table: books              │
           │ - id                      │
           │ - title                   │
           │ - author                  │
           │ - isbn                    │
           │ - status                  │
           │ - created_at              │
           └───────────────────────────┘

📝 Responsibilities of Each Layer
1️⃣ Presentation Layer

หน้าที่
รับ HTTP Request จาก Client
แปลงข้อมูลจาก Request
เรียกใช้ Business Logic Layer
ส่ง HTTP Response
จัดการ Error
ไฟล์
bookRoutes.js
bookController.js
errorHandler.js
ห้ามทำ
❌ เขียน SQL
❌ เขียน Business Rules

2️⃣ Business Logic Layer
หน้าที่
ตรวจสอบความถูกต้องของข้อมูล (Validation)
บังคับใช้กฎทางธุรกิจ
ประมวลผลข้อมูล
เรียกใช้ Data Access Layer
ไฟล์
bookService.js
bookValidator.js
Business Rules
ต้องมี title, author, isbn
หนังสือที่ถูกยืมแล้ว ไม่สามารถยืมซ้ำ
การคืนหนังสือต้องเปลี่ยนสถานะเป็น available

3️⃣ Data Access Layer
หน้าที่
ติดต่อฐานข้อมูล
ทำ CRUD Operations
แปลงข้อมูลจาก SQL เป็น Object
ไฟล์
bookRepository.js
connection.js
ห้ามทำ
❌ Business Logic
❌ Validation
❌ HTTP Handling

🔄 Data Flow Example: Borrow Book
Client
  ↓ PATCH /api/books/:id/borrow
Controller
  ↓ call bookService.borrowBook(id)
Service
  ↓ check status === available
Repository
  ↓ UPDATE books SET status='borrowed'
Database
  ↓ update record
Response
  ← updated book object

📌 Summary
Architecture Benefits
✅ Separation of Concerns
✅ Maintainability
✅ Testability
✅ Scalability
Key Principles
แต่ละ Layer มีหน้าที่ชัดเจน
Business Rules อยู่ใน Business Layer เท่านั้น
Presentation Layer ไม่เข้าถึง Database โดยตรง
